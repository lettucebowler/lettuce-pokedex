version: '3.3'

services:
  lettuce-pokedex:
    build: .
    container_name: lettuce-pokedex
    restart: unless-stopped
    environment:
      - TZ=America/Chicago
      - VITE_IMAGE_HOST=https://cdn.dex.rusondia.net
networks:
  default:
    external:
      name: nginx
